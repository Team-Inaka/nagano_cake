<%= form_with(model: costomers_cart_item, local: true) do |form| %>
  <% if costomers_cart_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(costomers_cart_item.errors.count, "error") %> prohibited this costomers_cart_item from being saved:</h2>

      <ul>
      <% costomers_cart_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
